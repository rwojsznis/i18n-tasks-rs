{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":dependencyDashboard",
    ":semanticCommits",
    "helpers:pinGitHubActionDigests"
  ],
  "labels": [
    "dependencies"
  ],
  "cargo": {
    "enabled": true
  },
  "github-actions": {
    "enabled": true
  },
  "customManagers": [
    {
      "customType": "regex",
      "description": "Update the minimum Rust version in Cargo.toml.",
      "managerFilePatterns": [
        "/(^|/)Cargo\\.toml$/"
      ],
      "matchStrings": [
        "rust-version\\s*=\\s*\"(?<currentValue>\\d+\\.\\d+(?:\\.\\d+)?)\""
      ],
      "datasourceTemplate": "rust-version",
      "depNameTemplate": "rust",
      "versioningTemplate": "rust"
    }
  ],
  "packageRules": [
    {
      "description": "Update the Rust toolchain and minimum supported version together.",
      "matchDatasources": [
        "rust-version"
      ],
      "matchPackageNames": [
        "rust"
      ],
      "groupName": "Rust toolchain",
      "groupSlug": "rust-toolchain"
    },
    {
      "description": "Automerge non-major dependency updates after required CI checks pass.",
      "matchUpdateTypes": [
        "patch",
        "minor",
        "digest",
        "pin"
      ],
      "automerge": true,
      "automergeType": "pr",
      "platformAutomerge": true,
      "rebaseWhen": "behind-base-branch"
    }
  ]
}
